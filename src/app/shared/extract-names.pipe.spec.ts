import { ExtractNamesPipe } from './extract-names.pipe';

describe('ExtractNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
